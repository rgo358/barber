# 🔄 Автоматическое Сохранение Проекта

Система автоматического сохранения всех изменений в Git репозиторий.

## 🚀 Быстрый старт

```bash
# Запустить автосохранение
./autosave.sh start

# Проверить статус
./autosave.sh status

# Показать логи (real-time)
./autosave.sh logs

# Остановить
./autosave.sh stop
```

## 📋 Как это работает?

1. **auto-commit.sh** — основной скрипт, который:
   - Каждые 5 минут проверяет наличие изменений (`git status`)
   - Если изменения есть → добавляет всё (`git add -A`)
   - Коммитит с меткой `🔄 Auto-save: [timestamp]`
   - Логирует всё в `auto-commit.log`

2. **autosave.sh** — контроллер для управления:
   - `start` — запустить фоновый процесс
   - `stop` — остановить
   - `status` — проверить статус
   - `logs` — показать логи в реальном времени

## 📊 Что сохраняется?

✅ **ВСЕ изменения:**
- Новые файлы
- Изменённые файлы
- Удалённые файлы
- Папки и структура

❌ **НЕ сохраняется** (благодаря `.gitignore`):
- `__pycache__/` — кэш Python
- `venv/`, `env/` — виртуальные окружения
- `.venv` — venv директория
- `*.log` — логи (кроме auto-commit.log)
- `.env` — переменные окружения
- `.ipynb_checkpoints/` — Jupyter кэш

## 🔍 Примеры логов

```
🔄 Инициализирую автоматическое сохранение...
📁 Репо: /workspaces/barber
⏱️  Интервал: 300s (5 минут)
📝 Логи: /workspaces/barber/auto-commit.log
---
[2025-11-14 21:45:30] ✅ Обнаружены изменения:
 M salon_bot.py
 M README.md
[2025-11-14 21:45:30] 🎉 Коммит: 3f8a1c2 - 🔄 Auto-save: 2025-11-14 21:45:30 | Changed: 2
---
```

## 🛠️ Настройка интервала

Отредактируйте `auto-commit.sh`:

```bash
COMMIT_INTERVAL=300  # Измените на нужное значение (в секундах)
# 60 = 1 минута
# 300 = 5 минут (по умолчанию)
# 1800 = 30 минут
```

## 📁 Файлы системы

- `autosave.sh` — контроллер (главный скрипт для пользователя)
- `auto-commit.sh` — основной логик сохранения
- `.auto-commit.pid` — файл с PID процесса (создаётся автоматически)
- `auto-commit.log` — файл логов

## ⚡ Расширенные команды

### Просмотр последних коммитов
```bash
git log --oneline -20 | grep "Auto-save"
```

### Удалить все Auto-save коммиты
```bash
git log --oneline | grep "Auto-save" | wc -l
```

### Упаковать логи
```bash
gzip auto-commit.log
```

## 🚨 Возможные проблемы

| Проблема | Решение |
|----------|----------|
| "Автосохранение уже запущено" | Используйте `./autosave.sh status` и `./autosave.sh stop` |
| Процесс "зависает" | `./autosave.sh restart` |
| Логи растут слишком быстро | Сжимайте: `gzip auto-commit.log` |
| Нет коммитов в течение 5+ минут | Проверьте: `git status` |

## 💾 Безопасность

⚠️ **Важно:**
- Автосохранение коммитит **ВСЁ**, включая чувствительные файлы
- **НИКОГДА** не коммитьте `.env` файлы (используйте `.gitignore`)
- Проверьте `.gitignore` перед запуском:
  ```bash
  cat .gitignore | grep -E "\.env|token|secret"
  ```

## 📞 Примеры использования

### Сценарий 1: Разработка без забот
```bash
# Один раз при старте проекта
./autosave.sh start

# Работаете спокойно - всё сохраняется автоматически
# Редактируете файлы, создаёте новые...

# Перед завершением сессии (опционально)
./autosave.sh stop
```

### Сценарий 2: Мониторинг в реальном времени
```bash
# Терминал 1: запустить автосохранение
./autosave.sh start

# Терминал 2: смотреть логи
./autosave.sh logs

# Терминал 3: работаете в проекте
# Изменяете файлы...
```

### Сценарий 3: Проверка перед сном
```bash
# Убедиться, что всё сохранено
./autosave.sh status

# Если активно - спокойно спить 😴
```

## 🎯 Интеграция с GitHub

Auto-save коммиты автоматически попадают в GitHub при следующем `git push`:

```bash
# Один раз загрузить все auto-save коммиты
git push origin main

# Или настроить автопуш (для продвинутых)
# git config alias.apush "push origin main"
```

---

**Вопросы?** Проверьте логи: `./autosave.sh logs`
